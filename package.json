{
  "name": "voice-arcana",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "check": "node --check src/main.js && node --test"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
