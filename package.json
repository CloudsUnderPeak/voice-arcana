{
  "name": "voice-arcana",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test \"test/*.test.js\"",
    "check": "node tools/check-syntax.mjs && npm test"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
